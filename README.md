# Rightme
package main

import "fmt"

var (
	product  = "mobile"
	price    = 2000
	quantity = 50
)
func main() {

	fmt.Println("mobiles costing each one at", price)
	fmt.Println("available quantity", quantity)

}
